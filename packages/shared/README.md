# Shared packages

This directory is reserved for contracts that genuinely need to be shared
across applications, such as generated GraphQL types. It intentionally contains
no hand-maintained duplicate domain models in Phase 0.
